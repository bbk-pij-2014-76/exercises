println (" Please enter a text ");
String str=readLine();
String str1 = "";
for (int i = str.length()-1; i>=0;  i--) {
	str1 = str1 + str.charAt(i);
}

if (str.equals(str1)) {
	println( " Its a strict palindrome ");
} else {
	println("Its not a strict palindrome ");
}